import { TablePagination } from '@material-ui/core';
import styled from 'styled-components';

export const TableStyles = {
  TablePagination: styled(TablePagination)`
    && {
      div {
        /* padding-left: 0;
        margin-left: 0; */
      }
    }
  `,
};
