import { FC } from 'react';
import { MainContainer } from '@components/container';

export const Stacking: FC = () => {
  return (
    <MainContainer>
      <div>Staking</div>
    </MainContainer>
  );
};
